import { ComingSoon } from "@/components/layout/coming-soon";

export default function Page() {
  return <ComingSoon title="Dispatch Board" description="Kanban, timeline, and list views for booked, covered, pickup, transit, and delivered loads — coming in the next pass." />;
}
